name = "airside"
description = "Perception behavior-tree engine (Part 4): pure py_trees behaviors tested on the host, ROS plumbing provided for the container."

depends_on = ["camera", "utils"]

[commands]
setup = "uv sync --extra dev"
test = "uv run --extra dev pytest"
lint = "uv run --extra dev ruff check ."

[ci]
pr = ["lint", "test"]

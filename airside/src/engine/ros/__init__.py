"""
The ROS code we wrote for you. Read it, don't change it.

Everything here imports rclpy at the top, so it only runs inside the Part 5
container. Your laptop tests never touch it. This is the layer that feeds
your behaviors real data instead of fixtures: ``telemetry`` and
``camera_source`` write the blackboard, ``perception_publisher`` and
``flight`` do the talking to the drone, ``mission`` ties it together.
"""
